[build]
python_version = "3.11.2"

[phases.install]
cmds = [
    "pip install -r requirements.txt",
]

[phases.build]
cmds = [
    "python manage.py collectstatic --noinput",
]

[start]
cmd = "gunicorn land_viewer.wsgi:application --bind 0.0.0.0:$PORT"
